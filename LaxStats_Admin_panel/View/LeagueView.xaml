<UserControl x:Class="LaxStats_Admin_panel.View.LeagueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LaxStats_Admin_panel.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Margin="10" Width="200" Content="Team" Command="{Binding UpdateViewCommand}" CommandParameter="Team"></Button>
        <Button Grid.Row="1" Margin="10" Width="200" Content="{Binding Napis}" Command="{Binding TestV2}" CommandParameter="Team"></Button>
    </Grid>
</UserControl>
